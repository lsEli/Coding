const x = 10; //Declaring a variable x and assigning it a value of 10
const y = 20; //Declaring a variable y and assigning it a value of 20
const z = x > y ? "x is greater than y" : "x is less than or equal to y"; //Declaring a variable z and assigning it a value of either "x is greater than y" or "x is less than or equal to y" depending on the result of the condition x > y

console.log(z); // x is less than or equal to y