let sum = (a, b, c) => { // Declaring a function sum
    return a + b + c; // Returning the sum of the three parameters
};

const array = [1, 2, 3]; // Declaring an array

console.log(sum(...array)); // 6