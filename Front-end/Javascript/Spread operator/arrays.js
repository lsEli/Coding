const array = [1, 2, 3]; // Declaring an array
const result = [...array, 4, 5, 6]; // Declaring a new array result that contains the elements of array and the elements 4, 5, 6

console.log(result); // [1, 2, 3, 4, 5, 6]